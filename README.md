# From Li_xin
* wmgit
```py
工作目錄下新增config.py
class config:
    #tbonline user
    tbuser = ''
    #tbonline passwd
    tbpasswd = ''
    user = '${user}'
    passwd = '${password}'
    domain = '${git_domain}'
    worktree = '${path}'
    gitpath = '${git_path}'
    conf_path = '${shell_config_path}'
    #是否為正式環境
    office = False

```

