# From Li_xin
* wmgit
```py
工作目錄下新增config.py
class config:
    #tbonline user
    tbuser = ''
    #tbonline passwd
    tbpasswd = ''
    #後台API user
    user = '${user}'
    #後台API passwd
    passwd = '${password}'
    #domain
    domain = '${git_domain}'
    #文件層
    worktree = '${path}'
    #git clone層
    gitpath = '${git_path}'
    #rsync conf層
    conf_path = '${shell_config_path}'
    #專案類別
    series = ''
    #是否為正式環境
    office = False

```

